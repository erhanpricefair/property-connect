# Database (Neon) — see docs/ARCHITECTURE.md §8.1/§8.8
DATABASE_URL="postgresql://user:password@host/db?sslmode=require&pgbouncer=true"
DIRECT_URL="postgresql://user:password@host/db?sslmode=require"

# Auth.js — generate with: npx auth secret
AUTH_SECRET=""

# Resend (email) — docs/ARCHITECTURE.md §9.1
RESEND_API_KEY=""
EMAIL_FROM="PropertyConnect <no-reply@propertyconnect.com.au>"

# Twilio (SMS) — docs/ARCHITECTURE.md §9.2
TWILIO_ACCOUNT_SID=""
TWILIO_AUTH_TOKEN=""
TWILIO_FROM_NUMBER=""

# PostHog (analytics) — docs/ARCHITECTURE.md §9.3
NEXT_PUBLIC_POSTHOG_KEY=""
NEXT_PUBLIC_POSTHOG_HOST="https://us.i.posthog.com"
POSTHOG_SERVER_API_KEY=""

# OpenAI (AI enrichment) — docs/ARCHITECTURE.md §9.4
OPENAI_API_KEY=""

# Inngest (background jobs) — docs/ARCHITECTURE.md §4.3/§8.8
INNGEST_EVENT_KEY=""
INNGEST_SIGNING_KEY=""

# Upstash Redis (rate limiting) — docs/ARCHITECTURE.md §7
UPSTASH_REDIS_REST_URL=""
UPSTASH_REDIS_REST_TOKEN=""
